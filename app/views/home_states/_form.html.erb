<%= form_for(home_state) do |f| %>
  <% if home_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home_state.errors.count, "error") %> prohibited this home_state from being saved:</h2>

      <ul>
      <% home_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
